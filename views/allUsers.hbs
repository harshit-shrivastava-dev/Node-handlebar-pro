<h1>All Users</h1>
{{#if users}}
    <ul>
        {{#each users}}
        <li>
            <strong>{{this.firstName}} {{this.lastName}}</strong> - 
            Phone: {{this.number}}, Location: {{this.location}}
            <a href="/edit/{{this._id}}">Edit</a>
            <form action="/delete/{{this._id}}" method="POST" style="display:inline;">
                <button type="submit">Delete</button>
            </form>
        </li>
        {{/each}}
    </ul>
{{else}}
    <p>No users found.</p>
{{/if}}
<a href="/">Go Back to Form</a>